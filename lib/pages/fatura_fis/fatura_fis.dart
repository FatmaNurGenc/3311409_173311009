import 'package:flutter/material.dart';

class FaturaFis extends StatelessWidget {
  const FaturaFis({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Fatura/Fiş")),
    );
  }
}

import 'package:flutter/material.dart';

class SiparisYonetimMenu extends StatelessWidget {
  const SiparisYonetimMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Sipariş Yönetimi")),
    );
  }
}

import 'package:flutter/material.dart';

class StokMenu extends StatelessWidget {
  const StokMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Stok")),
    );
  }
}
